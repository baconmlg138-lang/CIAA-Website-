# Contact form → Google Apps Script web app URL
# (from Deploy → Web app; keep the Google Sheet itself private)
VITE_CONTACT_FORM_URL=

# Must match the SHARED_SECRET Script Property in Apps Script
VITE_CONTACT_FORM_SECRET=
